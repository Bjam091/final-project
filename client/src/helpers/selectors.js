function getCurrentTrack(state, track) {
  if (!track) {
    return null;
  } else {
    const artist = track.artist;
      return { track, artist };
  }
}

function getRecentlyPlayed(user) {
 let tracks = [];

 return tracks;
}

